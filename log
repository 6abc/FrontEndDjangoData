<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Status Hover Panel</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #0b1220;
      color: #e5e7eb;
      padding: 40px;
    }

    .dots {
      display: flex;
      gap: 6px;
    }

    .dot {
      width: 20px;
      height: 40px;
      border-radius: 4px;
      cursor: pointer;
    }

    .ok { background: #22c55e; }
    .fail { background: #ef4444; }

    .panel {
      position: absolute;
      background: #020617;
      border: 1px solid #1e293b;
      border-radius: 6px;
      padding: 12px;
      width: 300px;
      font-size: 13px;
      display: none;
      z-index: 1000;
      box-shadow: 0 10px 30px rgba(0,0,0,.6);
    }
    .zoomer-wrapper {
      display: flex;
      justify-content: center;
    }

    .zoomer {
      transition: transform .2s ease;
      transform-origin: center;
    }

    .zoomer:hover {
      transform: scale(1.2);
    }
  </style>
</head>
<body>

<div class="zoomer-wrapper">
  <div class="zoomer">
    <h5>Recent Checks</h5>
    <div class="dots">
    <span class="dot ok" data-info="<strong>Timestamp</strong><br>2025-12-27 17:05:24<br><br><strong>Response Time</strong><br>19ms<br><br><strong>Conditions</strong><br>❌ [CONNECTED] (false) == true<br>✅ [STATUS] <= 299"></span>
    <span class="dot fail" data-info="❌ Timeout<br>229ms<br>2025-12-27 17:41"></span>
    <span class="dot fail" data-info="❌ TLS Failed<br>180ms<br>2025-12-27 17:42"></span>
    </div>
  </div>
</div>

<div id="panel" class="panel"></div>

<script>
  const panel = document.getElementById('panel');
  let pinned = false;

  document.querySelectorAll('.dot').forEach(dot => {

    dot.addEventListener('mouseenter', () => {
      if (pinned) return;
      show(dot);
    });

    dot.addEventListener('mouseleave', () => {
      if (!pinned) hide();
    });

    dot.addEventListener('click', (e) => {
      e.stopPropagation();
      pinned = true;
      show(dot);
    });
  });

  document.addEventListener('click', () => {
    pinned = false;
    hide();
  });

  function show(dot) {
    panel.innerHTML = dot.dataset.info;

    const r = dot.getBoundingClientRect();
    panel.style.top = r.bottom + window.scrollY + 8 + 'px';
    panel.style.left = r.left + window.scrollX + 'px';
    panel.style.display = 'block';
  }

  function hide() {
    panel.style.display = 'none';
  }
</script>

</body>
</html>
